# Specs

The project specifications live in [`.claude/specs/`](../.claude/specs/), tracked
alongside the implementation plans in [`.claude/plans/`](../.claude/plans/).

This directory exists to keep the `specs/` path from the specification's required
directory structure; it holds no content of its own.
